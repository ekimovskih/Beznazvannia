using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WorkBench_scr : MonoBehaviour
{//НЕНУЖОООООН!!!!!!!!!!!!!!
    /*
    public GameObject SlotPrefab = null;
    private Inventory_scr CraftInventory;
    public int NumberOfSlots = 2;
    private GameObject[] Slots;
    */


    private void Awake()
    {
        //CraftInventory = /*
        /*
        Debug.Log(this.gameObject.GetComponent<Inventory_scr>() == null);
        Slots = new GameObject[NumberOfSlots];
        for (int i = 0; i< NumberOfSlots; i++)
        {
            Slots[i] = Instantiate(SlotPrefab, this.gameObject.transform);
        }*/
        
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
